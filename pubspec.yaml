name: mystery_link
description: A game that connects two elements (A → Z) through a chain of intermediate links
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # State Management
  flutter_bloc: 
  equatable: 
  
  # JSON Serialization
  json_annotation: 
  
  # Localization
  intl: 
  
  # UI
  google_fonts: 
  lottie: 
  
  # Storage
  shared_preferences: 
  
  # QR Code & Sharing
  qr_flutter: 
  mobile_scanner: 
  share_plus: 
  url_launcher: 
  clipboard: 
  
  # WebSocket for Multiplayer
  web_socket_channel: 
  http: 
  
  # Time Zone Management
  timezone: 
  
  # Local Notifications
  flutter_local_notifications: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 
  build_runner: 
  json_serializable: 
  bloc_test:
  mocktail:

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/data/
    - assets/images/

  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf

